# Copy this file to .env and replace every placeholder before starting the API.
# Generate a different random value for each secret. Each must contain at least
# 32 UTF-8 bytes. One way to generate a value with Node.js is:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"

PORT=3000
DB_URL=mongodb://127.0.0.1:27017/account-api?replicaSet=rs0
FRONTEND_URL=http://localhost:5173
GOOGLE_CLIENT_ID=replace-me
JWT_SECRET=replace-me
OTP_SECRET=replace-me
LOGIN_THROTTLE_SECRET=replace-me
REQUEST_THROTTLE_SECRET=replace-me
